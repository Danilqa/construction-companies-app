import { memo } from 'react';
import * as React from 'react';
import { Section } from '../components/section/section';

export const CaseArrival = memo(() => (
    <Section.Container>
        <Section.Content>
            meaw meaw
        </Section.Content>
    </Section.Container>
));
