import { memo } from 'react';
import styled from 'styled-components';

export const Introduction = memo(styled.div`
    width: 590px;
`);

export const Header = memo(styled.div`
    position: absolute;
    right: 0;
`);
