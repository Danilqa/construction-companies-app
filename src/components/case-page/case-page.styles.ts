import { memo } from 'react';
import styled from 'styled-components';

export const CasePageContainer = memo(styled.div`
    display: flex;
`);

export const Title = memo(styled.div`
    font-size: 26px;
`);

export const Content = memo(styled.div`
`);
